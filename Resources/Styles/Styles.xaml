<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!--  Токены цвета (Dark Neon Theme)  -->
    <Color x:Key="BgPrimary">#06040A</Color>
    <Color x:Key="BgSecondary">#0A0714</Color>
    <Color x:Key="BgTertiary">#110C24</Color>

    <Color x:Key="TextPrimary">#ECE9FC</Color>
    <Color x:Key="TextSecondary">#9E96C4</Color>

    <Color x:Key="NeonCyan">#00F5FF</Color>
    <Color x:Key="NeonMagenta">#FF2A85</Color>
    <Color x:Key="NeonPurple">#7C3AED</Color>
    <Color x:Key="NeonGreen">#00FFC2</Color>
    <Color x:Key="NeonYellow">#FFD700</Color>

    <Color x:Key="BorderDark">#1D153A</Color>
    <Color x:Key="BorderLight">#2D1A54</Color>

    <!--  Стили по умолчанию  -->
    <Style ApplyToDerivedTypes="True" TargetType="Page">
        <Setter Property="BackgroundColor" Value="{StaticResource BgPrimary}" />
    </Style>

    <Style TargetType="Label">
        <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
        <Setter Property="FontFamily" Value="OpenSansRegular" />
    </Style>

    <!--  Базовый стиль кнопок в тулбаре  -->
    <Style x:Key="ToolbarButton" TargetType="Button">
        <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="FontFamily" Value="OpenSansSemibold" />
        <Setter Property="HeightRequest" Value="28" />
        <Setter Property="Padding" Value="8,0" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="#1A1138" />
                            <Setter Property="TextColor" Value="{StaticResource NeonCyan}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="#2D1A54" />
                            <Setter Property="Scale" Value="0.95" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <!--  Акцентная кнопка (Например, "Запуск")  -->
    <Style x:Key="AccentButton" TargetType="Button">
        <Setter Property="TextColor" Value="#FFFFFF" />
        <Setter Property="BackgroundColor" Value="#7C3AED" />
        <Setter Property="CornerRadius" Value="14" />
        <Setter Property="FontFamily" Value="OpenSansSemibold" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="#FF2A85" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="#120A28" />
                            <Setter Property="Scale" Value="0.93" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

</ResourceDictionary>
